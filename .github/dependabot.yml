version: 2
updates:
  - package-ecosystem: github-actions
    directories:
      - / # workflows + the root compute action.yml
      - /post # the post composite action.yml
    schedule:
      interval: monthly
    cooldown:
      default-days: 5
